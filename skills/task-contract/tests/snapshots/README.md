# Snapshot Directory

Status: v0.3.0 snapshot protocol directory.

This directory records structural snapshot expectations for `task-contract` fixture review.

Snapshots may document:

- expected mode;
- required sections;
- required fields;
- required checks;
- forbidden patterns;
- approval requirements.

Use one snapshot file per fixture when snapshots are added.
